{
  "name": "robios-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "smoke": "bun run scripts/smoke-test.ts",
    "export:duckdb": "python3 scripts/export_duckdb.py"
  },
  "dependencies": {
    "hono": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
